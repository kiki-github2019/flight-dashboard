## 1th review

### 1. 전체 수정 우선순위 원칙
1. **크래시 / 데이터 로딩 실패** (최우선, 사용자 워크플로우 차단)
2. **세션/Studio 안정성** (Project Explorer, multi-session)
3. **UI/UX 핵심 동작** (비디오-데이터 동기, 드래그 실시간성, visibility)
4. **시인성 / 레이아웃** (확대 시 깨짐, 색상 대비)
5. **개선/추가 기능** (슬라이더, 파일명 표시, 게이지 등)
6. **Legacy (FlightDataDashboard 단독 실행)**

---

### 2. 수정 계획 (Phase별 그룹화)

#### **Phase A: 즉시 수정 (High Risk / Blocking) - 1순위**
| 우선순위 | 문제 | 수정 내용 | 위험도 | 예상 소요 |
|----------|------|----------|--------|----------|
| 1 | CSV 로딩 시 "Required flight-data columns... Roll, Pitch" 에러 | `option1.dat` 형식과 테스트 CSV 헤더 매핑 로직 강화. Column mapper tolerant 모드 추가 (대소문자, 공백, 별칭 지원) | 높음 | 1일 |
| 2 | Project Explorer가 최초 실행 시 안 보임 ("Welcome" 탭만) | Studio 시작 시 기본 Session 1 자동 생성 + Project Explorer 자동 expand | 중 | 0.5일 |
| 3 | FlightDataDashboard 단독 실행 vs Studio 내 embedded 차이 | `FlightDataDashboard.m`을 Studio-compatible wrapper로 강화 (Session injection, shared services) | 중 | 1일 |
| 4 | Manager/Detail/ROI/Analyzer Figure 관리 문제 (활성화, Tab 연동, 사라짐) | Figure Manager Service 도입 (per-session figure registry + bringToFront) | 중-높음 | 2일 |

#### **Phase B: 동작 개선 (Medium Risk)**
| 우선순위 | 문제 | 수정 내용 |
|----------|------|----------|
| 5 | 비디오 플레이어 슬라이더 드래그 시 실시간 업데이트 안 됨 | `VideoPlayer`의 `ValueChangedFcn`을 `ContinuousValueChanged` (또는 timer + drawnow)로 개선 |
| 6 | 해안선 (lat/lon) 표시 범위 문제 | Axes `XLim`/`YLim`을 data extent에 맞춰 auto + padding 적용 |
| 7 | 확대 시 버튼/editbox 길이 늘어남 | `GridLayout` + `MinimumWidth`/`MaximumWidth` 고정 또는 `uipanel` inner layout 재설계 |
| 8 | Session 추가 시 초기 화면 ("현재비행정보", "H: Data View Panel"만) | Default visible panels 설정 + collapsible panels 정리 |
| 9 | ROI 버튼 → 오렌지 배경 후 동작 불명 + 드래그 부자연스러움 | ROI draw mode 명확화 (crosshair cursor, status message, escape key 지원) |

#### **Phase C: UI/시인성 개선 (Low Risk)**
- Panel title, slider 위 프레임 숫자 등 **배경/폰트 대비** 전수 조사 및 Theme 적용 (`uistyle` 또는 custom `Label` properties)
- Pitch/Roll/Heading 게이지 삼각형 지시자 크기·색상 강화 (scale with figure size)
- Flight 1 / Flight 2 파일명 GUI 상단 또는 Tab title에 표시
- 비행데이터 plot용 슬라이더 추가 (Video Player 영역 빈 공간 활용)
- "Range" 버튼: 기능 미구현이면 삭제 또는 placeholder → tooltip

#### **Phase D: 세부 개선 및 Legacy**
- FPS가 AVI마다 고정값(230/830) → VideoReader `FrameRate` 제대로 읽도록 수정
- Mouse drag 중 십자 커서 잔류 + freeze → MouseRouter lock 해제 강화 (already partially in tests)
- Detail 버튼 등 세부 기능 구체화 (Show checkbox 연동 등)

---

### 3. 수정 순서 추천 (안전 우선)

1. **Column Mapping Fix** (CSV 로딩) → 가장 먼저. 모든 테스트 데이터가 깨지면 진행 불가.
2. **Studio 초기화 & Project Explorer** + **Session 기본 visible panels**.
3. **FlightDataDashboard wrapper 개선** (단독 실행 호환성).
4. **Video Scrubbing 실시간성** + **Mouse Router 안정화**.
5. **Layout / Resize** (uigridlayout tuning).
6. **Figure Manager** (Manager/Detail/ROI).
7. **시인성 & 게이지 & 슬라이더 추가**.
8. **해안선 + 기타**.

**테스트 전략**:
- 매 수정 후 `runAllTestCodesWithCleanup` + `verifyPhase*` 실행
- 특히 Phase 3 (embedded), Phase 4 (Undo/Event isolation), multi-session stress test 필수
- 비디오+데이터 동기, drag 시나리오 수동 검증


## 2th review
**✅ Column Mapping Fix + Studio 초기화 + FlightDataDashboard 호환성 작업 시작**

사용자 피드백(특히 CSV 로딩 에러)과 현재 코드(`FlightDataLoader.m`, `AppConstants.m` 등)를 면밀히 검토한 후, **안전한 수정 순서**로 진행하겠습니다.

### 1. Column Mapping Fix (최우선, Blocking Issue)

**현재 문제점**:
- `option1.dat`에 의존도가 높음
- `Roll`, `Pitch` 등 필수 컬럼이 매핑 실패 → 에러 다이얼로그
- `inferRequiredColumn` 함수가 존재하지만, 대소문자/공백/실제 테스트 CSV 헤더와 매칭률이 낮음

**수정 계획 (FlightDataLoader.m 중심)**:

1. **REQ_KEYS 강화 + Aliases 확장**
   - `requiredColumnCandidates`에 더 많은 실전 별칭 추가 (e.g. `ROLL`, `pitch`, `RollAngle`, `PITCH_DEG` 등)

2. **Fallback Mapping Logic 추가** (option*.dat 없거나 실패 시)
   - Exact match → Normalized match → Fuzzy/Partial match (contains) 순서로 시도
   - 최소 Roll/Pitch/Time은 반드시 매핑되도록 강제 + 사용자에게 매핑 UI 제안 (추후)

3. **에러 메시지 개선**
   - "Required ... not mapped" 대신 **누락된 컬럼 목록 + 추천 후보** 표시
   - `validateRequiredColumns`에서 graceful degradation (부분 매핑 허용 + warning)

4. **option*.dat 자동 생성/템플릿 기능** (테스트 데이터용)

**먼저 수정할 파일**:
- `+flightdash/+model/FlightDataLoader.m` (inferRequiredColumn, validateRequiredColumns, applyOptionFile)
- `+flightdash/+util/AppConstants.m` (REQ_KEYS 확장)

**위험도**: 낮음 (로딩 전단계, 기존 option*.dat 사용자 호환 유지)

---

### 2. Studio 초기 화면 + Project Explorer (동시 진행 가능)

**문제**:
- 최초 실행 시 "Welcome" 탭만 보이고 Project Explorer 안 뜸

**수정 방향** (`+flightdash/+studio/`):
- `FlightReviewStudioApp.m` (또는 Studio entry)에서 Session 1 자동 생성
- Project Explorer `uitree` 자동 expand + 기본 노드 선택
- Default visible panels: "현재비행정보", "H: Data View Panel" + Video/Player 등 핵심만 On

---

### 3. FlightDataDashboard 단독 실행 호환성

**목표**:
- `FlightDataDashboard` 단독 실행 시 기존처럼 잘 동작
- `FlightReviewStudio` 내 embedded 탭으로도 동일 코드베이스 사용

현재 구조가 wrapper + embedded mode를 지원하도록 되어 있으므로, **Session injection**과 **parentContainer** 처리 강화.

---

### 즉시 실행할 수정 요청

아래 순서로 **코드 수정**을 진행해 주세요 (Pull Request 또는 직접 commit):

1. **Column Mapping 개선**부터 먼저 (가장 급함)
2. Studio 초기화 로직
3. FlightDataDashboard wrapper 안정화

## 3th review
**. `FlightDataLoader.m` 전체 검토 + Column Mapping Fix 패치 제안 완료**

### 현재 `FlightDataLoader.m` 핵심 분석
- `parseFlightData` → `applyOptionFile` → `inferRequiredColumn` → `validateRequiredColumns` 순서로 동작
- **문제 원인**:
  - `validateRequiredColumns`에서 **Roll, Pitch** 등 하나라도 매핑 실패 → 즉시 `error` 발생 (사용자 에러 dlg: "Required flight-data columns were not mapped : Roll, Pitch...")
  - `requiredColumnCandidates`에 있는 후보가 테스트 CSV 헤더와 매칭 안 되는 경우가 많음
  - `option%d.dat` 파일이 없거나 내용이 테스트 데이터와 맞지 않으면 inference만 의존 → 실패율 높음

### **Column Mapping Fix 제안 (안전한 강화 버전)**

**1. `+flightdash/+util/AppConstants.m` 수정** (REQ_KEYS 관련 상수 추가)

```matlab
% AppConstants.m 에 추가 추천
properties (Constant)
    REQ_KEYS = {'Time', 'Roll', 'Pitch', 'Heading', 'Alt', 'Lat', 'Lon'};
    
    % 새로 추가: 더 유연한 alias 관리 (추후 확장 용이)
    COLUMN_ALIASES = struct( ...
        'Roll',  {'roll', 'rollangle', 'phi', 'bank', 'roll_deg', 'rolldeg'}, ...
        'Pitch', {'pitch', 'pitchangle', 'theta', 'pitch_deg', 'pitchdeg'}, ...
        'Heading', {'heading', 'yaw', 'course', 'track', 'psi', 'hdg'}, ...
        ... % 다른 키들도 필요시 확장
    );
end
```

**2. `FlightDataLoader.m` 주요 수정 패치** (inferRequiredColumn + validate 개선)

아래 **diff 형태**로 주요 변경 부분을 제안합니다. 전체 파일 교체가 아닌 **핵심 메서드만 수정**하는 것을 추천합니다.

```matlab
% === 1. inferRequiredColumn 강화 (기존 + fuzzy matching 추가) ===
function colName = inferRequiredColumn(obj, reqKey, csvHeaders)
    colName = '';
    candidates = obj.requiredColumnCandidates(reqKey);
    
    % Normalize된 헤더 준비
    normHeaders = cellfun(@obj.normalizeHeaderName, csvHeaders, 'UniformOutput', false);
    
    % 1순위: Exact / Normalized match (기존)
    for cIdx = 1:numel(candidates)
        normCand = obj.normalizeHeaderName(candidates{cIdx});
        matchIdx = find(strcmpi(normHeaders, normCand), 1, 'first');  % case-insensitive 강화
        if ~isempty(matchIdx)
            colName = csvHeaders{matchIdx};
            return;
        end
    end
    
    % 2순위: Partial / Contains match (새로 추가 - 강력 fallback)
    reqLower = lower(reqKey);
    for hIdx = 1:numel(csvHeaders)
        hNorm = lower(normHeaders{hIdx});
        if contains(hNorm, reqLower) || contains(reqLower, hNorm)
            colName = csvHeaders{hIdx};
            return;
        end
    end
end
```

```matlab
% === 2. validateRequiredColumns 개선 (에러 → Warning + Partial 허용) ===
function validateRequiredColumns(obj, dataTbl, mappedCols, fIdx)
    reqKeys = flightdash.util.AppConstants.REQ_KEYS;
    vars = dataTbl.Properties.VariableNames;
    missing = {};
    
    for i = 1:numel(reqKeys)
        keyName = reqKeys{i};
        mapped = mappedCols.(keyName);
        if isempty(mapped) || ~ismember(mapped, vars)
            missing{end+1} = keyName;
        end
    end
    
    if ~isempty(missing)
        msg = sprintf('Missing required columns: %s.\n' + ...
                     'Attempted auto-mapping, but recommend creating option%d.dat.', ...
                     strjoin(missing, ', '), fIdx);
        
        % Critical ones (Time, Lat, Lon, Alt)은 error 유지
        critical = {'Time','Lat','Lon','Alt'};
        critMissing = intersect(missing, critical);
        if ~isempty(critMissing)
            error('flightdash:DataMapping:MissingCriticalColumn', ...
                '%s\nCritical columns missing.', msg);
        else
            % Roll, Pitch, Heading 등은 warning + 계속 진행
            warning('flightdash:DataMapping:MissingOptionalColumn', msg);
        end
    end
end
```

**추가 추천**: `applyOptionFile`에서 option 파일이 없을 때 **자동 option%d.dat 템플릿 생성** 함수 추가 (사용자 편의).

---

**B. FlightReviewStudioApp.m 관련 (간단 검토)**

최초 실행 시 Project Explorer가 안 보이는 문제는 `FlightReviewStudioApp` constructor와 `addSession`, `refreshExplorer`에서 Session 1을 자동 생성하고 Explorer를 expand하는 로직을 보강하면 해결됩니다.  
`addSession` 메서드가 이미 잘 되어 있지만, **생성자에서 기본 Session 1 자동 추가**를 넣는 것이 좋습니다.

필요하면 이 파일도 다음으로 전체 리뷰 + 수정안 드리겠습니다.

---

**C. CHANGELOG + Issue 등록**

**CHANGELOG.md**에 아래 섹션 추가 추천:

```markdown
## [Unreleased] - 2026-05-14

### Fixed
- **Column Mapping** 대폭 강화 (`FlightDataLoader.m`)
  - Roll/Pitch 등 필수 컬럼 inference 개선 (exact + partial match)
  - option*.dat 미존재 시 graceful degradation (warning + 진행)
  - 에러 메시지 상세화

## 4th review
### Studio 초기화 + FlightDataDashboard 단독 실행 호환성 수정 계획 완료**

### 1. Studio 초기화 문제 (Project Explorer 안 보임 + Welcome 탭만)

**원인 분석** (현재 코드 기준):
- `FlightReviewStudioApp` constructor에서 `Project = ProjectModel('Untitled')` 생성 → `buildShell()` 호출.
- `addSession()`은 Menu나 버튼으로만 호출됨.
- **최초 실행 시 Session 1이 자동 생성되지 않음** → Project Explorer에 Sessions 노드가 비어있거나 Welcome/빈 탭만 보임.
- `refreshExplorer()`는 Session이 있어야 의미 있게 동작.

**수정 제안** (`FlightReviewStudioApp.m`)

**Constructor 끝부분에 자동 Session 1 생성 추가** (가장 안전한 위치):

```matlab
function app = FlightReviewStudioApp()
    try
        app.Project = flightdash.project.ProjectModel('Untitled');
        app.ensureSharedServices();
        app.ensureUndoServices();
        app.buildShell();
        app.applyGuiMode(app.Project.GuiMode, false);
        app.refreshTitle();

        % === [추가] 최초 실행 시 기본 Session 1 자동 생성 ===
        if app.Project.sessionCount() == 0
            app.addSession('Session 1');   % displayName 지정
            % 첫 번째 Session을 Active로 설정
            if ~isempty(app.Workspace) && isvalid(app.Workspace)
                sessions = app.Project.Sessions;
                if ~isempty(sessions)
                    app.Workspace.selectSession(sessions(1).SessionId);
                end
            end
        end
        % ================================================

        app.refreshExplorer();   % ← 이 호출을 constructor 끝에 명시적으로 추가
    catch ME
        ...
    end
end
```

**추가 개선**:
- `ProjectExplorerPanel.m`의 `refreshFromProject`에서 `expand` 호출 강화.
- `WorkspaceManager`의 초기 탭 생성 로직 확인 (Session 1 대시보드 자동 embed).

---

### 2. FlightDataDashboard 단독 실행 호환성 강화

**현재 구조**:
- `FlightDataDashboard.m` (root) → `flightdash.FlightDataDashboard()` wrapper
- Studio 내에서는 `WorkspaceManager.addDashboardTab()`에서 embedded mode로 생성

**문제**: 단독 실행 시와 Studio 내에서 동작/레이아웃 차이가 큼 (사용자 피드백).

**수정 방향** (`+flightdash/FlightDataDashboard.m` 또는 내부 클래스):

1. **Mode 감지 및 통합**:
   ```matlab
   properties
       IsEmbedded logical = false;     % Studio에서 호출된 경우 true
       ParentTab     % uitab (embedded일 때)
       StudioApp     % flightdash.studio.FlightReviewStudioApp (injection)
   end
   ```

2. **생성자 강화**:
   ```matlab
   function app = FlightDataDashboard(varargin)
       % Standalone vs Embedded 모드 구분
       if nargin > 0 && isa(varargin{1}, 'flightdash.studio.FlightReviewStudioApp')
           app.StudioApp = varargin{1};
           app.IsEmbedded = true;
           % embedded layout (panels into tab)
       else
           app.IsEmbedded = false;
           % standalone uifigure
       end
       ...
   end
   ```

3. **Layout 분기**:
   - Standalone: 독립 `uifigure` + full grid
   - Embedded: `parentContainer` (uitab 또는 uipanel) 받아서 내부에 panels 배치

**권장 구현**:
- `FlightDataDashboard` 내부에 `createStandaloneUI()` / `embedIntoContainer(parent)` 메서드 분리.
- Studio에서 호출할 때 `dashboard.embedIntoContainer(tabGrid)` 형태로 호출.

---

### 즉시 적용 추천 순서 (안전성 우선)

1. **FlightReviewStudioApp.m** 수정 (Session 1 자동 생성 + refreshExplorer 강제 호출)
2. **ProjectExplorerPanel.m** — 초기 expand 및 Session 1 선택 보강
3. **FlightDataDashboard** wrapper 및 embedded mode 강화 (단독 실행 호환)

## 5th review

**✅ A. `FlightReviewStudioApp.m` 전체 코드 검토 + Session 1 자동 생성 패치 제안 완료**

### FlightReviewStudioApp.m 검토 요약 (2026-05-13 최신)
- **현재 Constructor** (lines 74~89): `Project` 생성 → `ensureSharedServices` → `buildShell` → `applyGuiMode` → `refreshTitle` 순서.
- **Session 자동 생성 없음** → 사용자가 "Add Session"을 수동으로 눌러야 Project Explorer에 내용이 보임 (사용자 버그 리포트와 정확히 일치).
- `addSession` 메서드는 이미 잘 구현됨 (ProjectModel + Workspace embed).
- `refreshExplorer` 메서드 존재.

### **Session 1 자동 생성 패치 (Constructor에 추가 추천)**

**`FlightReviewStudioApp.m`의 `function app = FlightReviewStudioApp()` 끝부분 (try 블록 안, `refreshTitle();` 직후)에 아래 코드 삽입:**

```matlab
        function app = FlightReviewStudioApp()
            try
                app.Project = flightdash.project.ProjectModel('Untitled');
                app.ensureSharedServices();
                app.ensureUndoServices();
                app.buildShell();
                app.applyGuiMode(app.Project.GuiMode, false);
                app.refreshTitle();

                % ==================== [NEW] Studio 초기화 개선 ====================
                % 최초 실행 시 Session 1 자동 생성 + Project Explorer 활성화
                if app.Project.sessionCount() == 0
                    fprintf('Studio: Auto-creating default Session 1...\n');
                    defaultId = app.addSession('Session 1');  % addSession 내부에서 Workspace embed 호출됨
                    
                    % Active Session 설정
                    app.ActiveSessionId = defaultId;
                    
                    % Explorer 강제 refresh + expand
                    if ~isempty(app.ProjectExplorer) && isvalid(app.ProjectExplorer)
                        app.refreshExplorer();
                        % Tree expand (ProjectExplorerPanel에 expandAll 또는 selectNode 메서드 활용)
                        if ismethod(app.ProjectExplorer, 'expandAll')
                            app.ProjectExplorer.expandAll();
                        end
                    end
                    
                    % Workspace 첫 번째 탭 활성화
                    if ~isempty(app.Workspace) && isvalid(app.Workspace)
                        app.Workspace.setActiveTab(1);
                    end
                end
                % =================================================================
                
            catch ME
                ...
            end
        end
```

**추가 보강 추천 (같은 파일 내)**:
- `refreshExplorer` 메서드 끝부분에 `drawnow` 추가하여 UI 즉시 반영.
- `ProjectExplorerPanel.m`의 `refreshFromProject`에서 Session 노드 `expand` 로직 강화.

---

**B. WorkspaceManager.m + FlightDataDashboard embedded 관련 검토**

**WorkspaceManager.m**: raw 접근 시 404가 발생했으나, tree에서 파일 존재 확인됨. 주요 역할은 `addDashboardTab(sessionId, displayName)`로, Studio 내에서 FlightDataDashboard를 uitab에 embed하는 핵심입니다.

**FlightDataDashboard.m** (embedded 지원 현황 — 매우 양호):
- Constructor 이미 **embedded mode 지원** (`parentContainer`, `sessionId` 인자 처리)
  ```matlab
  function app = FlightDataDashboard(parentContainer, sessionId)
  ```
- `IsEmbedded`, `ActiveSessionId`, `RootContainer` 속성 준비됨.
- `UseSharedDecodeService`, `SharedCacheService` 등 Phase 10 prototype도 injection 준비.

**현재 호환성 문제점**:
- Standalone (`FlightDataDashboard()`) vs Embedded (`FlightDataDashboard(tab, sessionId)`)의 Layout 차이.
- Studio에서 호출할 때 `RootContainer`를 제대로 전달하지 못하는 경우가 있을 수 있음.

**FlightDataDashboard embedded 강화 패치 제안 (간단)**:

`FlightDataDashboard` constructor 상단에:

```matlab
            if embeddedMode
                app.IsEmbedded = true;
                app.ActiveSessionId = char(sessionId);
                app.RootContainer = parentContainer;
                % Embedded에서는 UIFigure을 Studio figure로 연결 (중요!)
                if isprop(parentContainer, 'Parent') && ~isempty(parentContainer.Parent)
                    app.UIFigure = ancestor(parentContainer, 'uifigure');
                end
            else
                % Standalone
                app.IsEmbedded = false;
                app.ActiveSessionId = 'standalone';
                app.createStandaloneFigure();  % 별도 메서드로 분리 추천
            end
```

**WorkspaceManager.addDashboardTab**에서 호출 예시:
```matlab
function addDashboardTab(obj, sessionId, displayName)
    tab = uitab(obj.WorkspaceTabGroup, 'Title', displayName);
    dash = flightdash.FlightDataDashboard(tab, sessionId);  % ← parent + sessionId 전달
    % tab.UserData = dash; 등으로 관리
end
```

## 6th review
**✅ ProjectExplorerPanel.m + WorkspaceManager.m 검토 완료**

### 1. **ProjectExplorerPanel.m** — refresh/expand 보강 패치

**현재 상태 분석**:
- `refreshFromProject`에서 `expand(obj.Roots.Project)`와 `expand(obj.Roots.Sessions)`를 이미 호출하고 있음.
- 그러나 **최초 Session 자동 생성** 후 Explorer가 제대로 expand되지 않거나, Session 노드가 보이지 않는 문제가 발생할 수 있음 (트리 rebuild 타이밍, `drawnow` 부족).

**보강 패치 제안** (`refreshFromProject` 메서드 끝부분 교체):

```matlab
        function refreshFromProject(obj, project)
            try
                if isempty(obj.Tree) || ~isvalid(obj.Tree), return; end
                if isempty(obj.Roots) || ~isfield(obj.Roots, 'Project'), return; end

                % Update root label
                if ~isempty(project) && ~isempty(project.ProjectName)
                    obj.Roots.Project.Text = project.ProjectName;
                end

                % --- Sessions ---
                obj.replaceChildren(obj.Roots.Sessions);
                if ~isempty(project) && ~isempty(project.Sessions)
                    for k = 1:numel(project.Sessions)
                        s = project.Sessions(k);
                        node = uitreenode(obj.Roots.Sessions, ...
                            'Text', sprintf('%s (%s)', s.DisplayName, s.SessionId), ...
                            'NodeData', struct('SessionId', s.SessionId, 'Kind', 'session'));
                    end
                end

                % ... (Themes, Results 등 기존 코드 유지)

                % ==================== [EXPAND 강화] ====================
                drawnow limitrate;   % UI 업데이트 강제
                
                try, expand(obj.Roots.Project); catch, end
                try, expand(obj.Roots.Sessions); catch, end
                try, expand(obj.Roots.Roi); catch, end
                
                % Session이 하나 이상 있으면 첫 번째 Session 자동 선택
                if ~isempty(project) && ~isempty(project.Sessions)
                    obj.selectSession(project.Sessions(1).SessionId);
                end
                % ====================================================
            catch
            end
        end
```

**추가 추천**:
- `FlightReviewStudioApp.m`의 Constructor에서 `addSession` 호출 후 **즉시** `app.ProjectExplorer.refreshFromProject(app.Project);` 호출.
- `selectSession` 메서드에 `drawnow`가 이미 있음 → 좋음.

---

### 2. **WorkspaceManager.m** 전체 검토 결과

**강점**:
- `addDashboardTab`가 잘 구현됨 (`FlightDataDashboard(tab, sessionId)` 형태로 embedded 지원).
- `DashboardEntries` (containers.Map)로 세션-탭-대시보드 매핑 관리 우수.
- `onTabChanged`, `refreshActiveLayout`, `activeSessionId` 등 Session isolation 준비가 잘 되어 있음 (Phase 3/4 호환).

**개선 필요 포인트**:
1. **WelcomeTab**이 기본으로 남아있고, Session 1 추가 후에도 WelcomeTab이 SelectedTab으로 남아있을 가능성 있음.
2. `addDashboardTab` 호출 후 `onTabChanged()`가 호출되지만, 최초 Studio 로드 시 **Welcome → Dashboard 전환**이 명확하지 않음.
3. Layout refresh가 `refreshActiveLayout`에 의존 → embedded 대시보드의 resize 대응이 중요.

**WorkspaceManager.m 보강 제안** (`addDashboardTab` 끝부분):

```matlab
                obj.DashboardEntries(sessionId) = struct(...);

                obj.TabGroup.SelectedTab = tab;
                obj.onTabChanged();   % ← 이미 있음 (좋음)

                % [추가] WelcomeTab이 active이면 Dashboard로 강제 전환
                if ~isempty(obj.WelcomeTab) && isvalid(obj.WelcomeTab) && ...
                   isequal(obj.TabGroup.SelectedTab, obj.WelcomeTab)
                    obj.TabGroup.SelectedTab = tab;
                end
```

**build 메서드**에서 WelcomeTab을 **항상 마지막**에 두거나, Session이 추가되면 숨기는 로직도 고려 가능.

---

**현재 추천 실행 순서**

1. **ProjectExplorerPanel.m**에 위 `refreshFromProject` expand + selectSession 보강 적용.
2. **FlightReviewStudioApp.m** Constructor에 Session 1 자동 생성 + `refreshExplorer()` 호출 (이전 제안).
3. **WorkspaceManager.m**에 WelcomeTab 강제 전환 로직 추가.
4. Column Mapping Fix와 함께 테스트 (`runAllTestCodesWithCleanup` + 수동 Studio 실행).

## 7th report
**✅ 요청하신 4개 항목 모두 검토 및 패치 제안 완료했습니다.**

### 1. ProjectExplorerPanel.m — refresh/expand 보강 (이미 잘 되어 있음 + 강화)

현재 `refreshFromProject` 메서드가 expand를 호출하고 있지만, **최초 Session 자동 생성 후** 타이밍 이슈로 expand/select가 안 될 가능성이 있습니다.

**보강 패치** (`refreshFromProject` 메서드 끝부분, `try, expand...` 블록 아래에 추가):

```matlab
                % ==================== [강화] 최초/자동 생성 후 expand + select ====================
                drawnow limitrate;  % UI 강제 갱신

                try, expand(obj.Roots.Project); catch, end
                try, expand(obj.Roots.Sessions); catch, end
                try, expand(obj.Roots.Roi); catch, end

                % Session이 있으면 첫 번째 Session 자동 선택 (Project Explorer에서 보이게)
                if ~isempty(project) && ~isempty(project.Sessions)
                    firstSessionId = project.Sessions(1).SessionId;
                    obj.selectSession(firstSessionId);   % 이미 구현된 메서드 재사용
                end
                % =================================================================
```

**추가 추천**: `build` 메서드 끝부분에도 `drawnow limitrate;` 추가.

---

### 2. WorkspaceManager.m 전체 패치 제안 (주요 개선)

**주요 개선 포인트**:
- WelcomeTab이 Session 추가 후에도 남아있을 수 있음
- `addDashboardTab` 후 강제 전환
- `onTabChanged` 안정화

**WorkspaceManager.m**에 적용할 주요 패치:

**`addDashboardTab` 메서드 끝부분 (`obj.onTabChanged();` 직후)**:

```matlab
                obj.TabGroup.SelectedTab = tab;
                obj.onTabChanged();

                % [추가] WelcomeTab이 여전히 active이면 Dashboard로 강제 전환
                if ~isempty(obj.WelcomeTab) && isvalid(obj.WelcomeTab) && ...
                   isequal(obj.TabGroup.SelectedTab, obj.WelcomeTab)
                    obj.TabGroup.SelectedTab = tab;
                    obj.onTabChanged();   % 다시 호출
                end
```

**`build` 메서드의 WelcomeTab 생성 후** (선택사항):

```matlab
            % WelcomeTab을 가장 뒤로 이동하거나, Session 추가 시 숨김 고려
            obj.TabGroup.SelectedTab = obj.WelcomeTab;  % 초기에는 Welcome
```

**`onTabChanged` 메서드 시작부분에** `drawnow limitrate;` 추가 추천.

---

### 3. FlightDataDashboard.m embedded mode 검토 결과

**현재 상태 (매우 양호)**:
- Constructor에서 `embeddedMode`를 제대로 감지하고 `IsEmbedded`, `ActiveSessionId`, `RootContainer` 설정.
- `WorkspaceManager.addDashboardTab`에서 `flightdash.FlightDataDashboard(tab, sessionId)` 형태로 호출 → 잘 연동됨.
- `LayoutMgr`, `refreshLayout`, responsive 관련 속성들도 준비됨.

**추가 보강 추천** (embedded 안정화):

`FlightDataDashboard` constructor, `embeddedMode` 블록 안에 아래 추가:

```matlab
                app.IsEmbedded      = true;
                app.ActiveSessionId = char(sessionId);
                app.RootContainer   = parentContainer;
                
                % Embedded 모드에서는 Studio의 UIFigure 연결
                if ~isempty(parentContainer) && isvalid(parentContainer)
                    fig = ancestor(parentContainer, 'matlab.ui.Figure');
                    if ~isempty(fig)
                        app.UIFigure = fig;
                    end
                end
```

**`createLayout` 또는 `startupFcn`**에서:
```matlab
            if app.IsEmbedded
                % parentContainer를 layout root로 사용
                parentForLayout = app.RootContainer;
            else
                parentForLayout = app.UIFigure;
            end
```

---

### 4. Column Mapping Fix + 위 수정 통합 테스트 계획

**Column Mapping Fix** (FlightDataLoader.m)는 이전에 제안한 대로 진행 (Partial match 강화 + critical/non-critical 구분).

**통합 적용 및 테스트 순서** (안전 우선):

1. **Column Mapping Fix** 먼저 적용 (CSV 로딩 에러 해결)
2. **ProjectExplorerPanel.m** refresh 강화
3. **FlightReviewStudioApp.m** Constructor에 Session 1 자동 생성 (이전 제안)
4. **WorkspaceManager.m** WelcomeTab 강제 전환 패치
5. **FlightDataDashboard.m** embedded UIFigure 연결 보강

**테스트 체크리스트**:
- Studio 단독 실행 (`FlightReviewStudio.m`) → **Project Explorer**에 Session 1 자동 표시 + Dashboard 탭 활성화 확인
- "Flight 1" 버튼 → CSV 선택 → Roll/Pitch 매핑 에러 사라지는지 확인
- FlightDataDashboard 단독 실행 (`FlightDataDashboard`) → 기존처럼 정상 동작 확인
- Session 추가/전환 시 레이아웃 깨짐 없는지
- `runAllTestCodesWithCleanup` 실행 후 `verifyPhase*` 통과 확인

## 8th report
**✅ 전체 패치 적용 시작** (2026-05-14 기준)

아래 **3개 핵심 패치**를 **commit/PR 형태**로 정리했습니다.  
**적용 순서**: Column Mapping Fix → Studio 초기화 (FlightReviewStudioApp) → Explorer/Workspace 강화

---

### **1. Column Mapping Fix 최종 diff** (`FlightDataLoader.m` + `AppConstants.m`)

#### **AppConstants.m** (COLUMN_ALIASES 추가)

```diff
--- a/+flightdash/+util/AppConstants.m
+++ b/+flightdash/+util/AppConstants.m
@@ -15,7 +15,20 @@ classdef AppConstants
         MAX_PLOTS_PER_TAB     = 12
         ...
         REQ_KEYS              = {'Time', 'Roll', 'Pitch', 'Heading', 'Alt', 'Lat', 'Lon'};
+
+        % === Column Mapping Aliases (새로 추가) ===
+        COLUMN_ALIASES = struct( ...
+            'Time',    {'time', 'timestamp', 't', 'sec', 'seconds'}, ...
+            'Roll',    {'roll', 'rollangle', 'phi', 'bank', 'roll_deg', 'rolldeg', 'RollAngle'}, ...
+            'Pitch',   {'pitch', 'pitchangle', 'theta', 'pitch_deg', 'pitchdeg', 'PitchAngle'}, ...
+            'Heading', {'heading', 'yaw', 'course', 'track', 'hdg', 'psi', 'HeadingAngle'}, ...
+            'Alt',     {'alt', 'altitude', 'height', 'alt_ft', 'altitude_ft'}, ...
+            'Lat',     {'lat', 'latitude', 'lat_deg'}, ...
+            'Lon',     {'lon', 'longitude', 'long', 'lon_deg'} ...
+        );
     end
 end
```

#### **FlightDataLoader.m** — `inferRequiredColumn` + `validateRequiredColumns` 강화

**`inferRequiredColumn` 메서드 전체 교체** (기존 메서드 찾아서 교체):

```matlab
        function colName = inferRequiredColumn(obj, reqKey, csvHeaders)
            % 강화된 Column Inference: Exact → Normalized → Partial match
            colName = '';
            if isempty(csvHeaders), return; end
            
            normHeaders = cellfun(@(h) lower(strtrim(h)), csvHeaders, 'UniformOutput', false);
            reqLower = lower(strtrim(reqKey));
            
            % 1. Exact / Alias match (AppConstants.COLUMN_ALIASES 사용)
            aliases = flightdash.util.AppConstants.COLUMN_ALIASES;
            if isfield(aliases, reqKey)
                cands = [reqKey, aliases.(reqKey)];
            else
                cands = {reqKey};
            end
            
            for c = cands
                candNorm = lower(strtrim(c{1}));
                idx = find(strcmp(normHeaders, candNorm), 1, 'first');
                if ~isempty(idx)
                    colName = csvHeaders{idx};
                    return;
                end
            end
            
            % 2. Partial / Contains match (fallback)
            for i = 1:numel(normHeaders)
                if contains(normHeaders{i}, reqLower) || contains(reqLower, normHeaders{i})
                    colName = csvHeaders{i};
                    return;
                end
            end
        end
```

**`validateRequiredColumns` 메서드 개선** (기존 메서드 찾아서 교체):

```matlab
        function validateRequiredColumns(obj, dataTbl, mappedCols, fIdx)
            reqKeys = flightdash.util.AppConstants.REQ_KEYS;
            missing = {};
            
            for i = 1:numel(reqKeys)
                key = reqKeys{i};
                mapped = mappedCols.(key);
                if isempty(mapped) || ~ismember(mapped, dataTbl.Properties.VariableNames)
                    missing{end+1} = key;
                end
            end
            
            if ~isempty(missing)
                msg = sprintf('Required columns not fully mapped: %s\n' + ...
                             'File: option%d.dat or headers mismatch.', ...
                             strjoin(missing, ', '), fIdx);
                
                critical = {'Time','Lat','Lon','Alt'};
                if ~isempty(intersect(missing, critical))
                    error('flightdash:DataMapping:MissingCritical', msg);
                else
                    warning('flightdash:DataMapping:MissingOptional', msg);
                end
            end
        end
```

---

### **2. FlightReviewStudioApp.m 전체 Constructor + Session 1 자동 생성 최종 패치**

**Constructor 전체 교체 추천** (`function app = FlightReviewStudioApp()` 부분):

```matlab
        function app = FlightReviewStudioApp()
            try
                % Initialize an empty project before any UI accesses it.
                app.Project = flightdash.project.ProjectModel('Untitled');
                app.ensureSharedServices();
                app.ensureUndoServices();
                app.buildShell();
                app.applyGuiMode(app.Project.GuiMode, false);
                app.refreshTitle();

                % ==================== [2026-05-14] Studio 초기화 개선 ====================
                % 최초 실행 시 Session 1 자동 생성 + Project Explorer 활성화
                if app.Project.sessionCount() == 0
                    fprintf('[Studio] Auto-creating default Session 1...\n');
                    defaultId = app.addSession('Session 1');
                    app.ActiveSessionId = defaultId;
                    
                    % Explorer 강제 refresh + expand
                    if ~isempty(app.ProjectExplorer) && isvalid(app.ProjectExplorer)
                        app.refreshExplorer();
                        if ismethod(app.ProjectExplorer, 'refreshFromProject')
                            app.ProjectExplorer.refreshFromProject(app.Project);
                        end
                    end
                    
                    % Workspace 첫 번째 탭 활성화
                    if ~isempty(app.Workspace) && isvalid(app.Workspace)
                        app.Workspace.setActiveTab(1);
                    end
                end
                % =================================================================
                
            catch ME
                if ~isempty(app.UIFigure) && isvalid(app.UIFigure)
                    delete(app.UIFigure);
                end
                rethrow(ME);
            end
        end
```

---

### **적용 후 추천 커밋 메시지**

```bash
git commit -m "fix: Column Mapping 강화 + Studio 초기화 개선 (Session 1 Auto-create)

- FlightDataLoader: Partial match + COLUMN_ALIASES 추가 (Roll/Pitch 매핑 실패 해결)
- FlightReviewStudioApp: Constructor에서 Session 1 자동 생성
- ProjectExplorer refresh/expand 보강
- WelcomeTab → Dashboard 자동 전환 준비"
```

---
## 9th report
**✅ ProjectExplorerPanel.m + WorkspaceManager.m 추가 패치 완료**

아래는 **현재 최신 코드**를 기반으로 한 **정확한 패치**입니다.

---

### **1. ProjectExplorerPanel.m 추가 패치** (refreshFromProject 강화)

**`refreshFromProject` 메서드 전체 교체 추천** (기존 메서드 교체):

```matlab
        function refreshFromProject(obj, project)
            % Rebuild the tree from a flightdash.project.ProjectModel.
            try
                if isempty(obj.Tree) || ~isvalid(obj.Tree), return; end
                if isempty(obj.Roots) || ~isfield(obj.Roots, 'Project'), return; end

                % Update root label
                if ~isempty(project) && ~isempty(project.ProjectName)
                    obj.Roots.Project.Text = project.ProjectName;
                end

                % --- Sessions ---
                obj.replaceChildren(obj.Roots.Sessions);
                if ~isempty(project) && ~isempty(project.Sessions)
                    for k = 1:numel(project.Sessions)
                        s = project.Sessions(k);
                        node = uitreenode(obj.Roots.Sessions, ...
                            'Text', sprintf('%s (%s)', s.DisplayName, s.SessionId), ...
                            'NodeData', struct('SessionId', s.SessionId, 'Kind', 'session'));
                    end
                end

                % --- Analysis Themes ---
                obj.replaceChildren(obj.Roots.Themes);
                if ~isempty(project) && ~isempty(project.AnalysisThemes)
                    for k = 1:numel(project.AnalysisThemes)
                        t = project.AnalysisThemes(k);
                        uitreenode(obj.Roots.Themes, ...
                            'Text', t.ThemeName, ...
                            'NodeData', struct('ThemeId', t.ThemeId, 'Kind', 'theme'));
                    end
                end

                % --- Review / Analysis Results ---
                obj.replaceChildren(obj.Roots.Roi);
                obj.replaceChildren(obj.Roots.Sync);
                obj.replaceChildren(obj.Roots.Snapshots);
                if ~isempty(project) && ~isempty(project.Results)
                    for k = 1:numel(project.Results)
                        r = project.Results(k);
                        parentNode = obj.resultRootFor(r);
                        uitreenode(parentNode, ...
                            'Text', obj.resultLabel(r), ...
                            'NodeData', struct('ResultId', r.ResultId, ...
                                'SessionId', r.SessionId, 'Kind', 'result'));
                    end
                end

                % ==================== [2026-05-14 강화] ====================
                drawnow limitrate;   % UI 즉시 반영

                try, expand(obj.Roots.Project); catch, end
                try, expand(obj.Roots.Sessions); catch, end
                try, expand(obj.Roots.Roi); catch, end

                % Session이 존재하면 첫 번째 Session 자동 선택 (Explorer에서 보이게)
                if ~isempty(project) && ~isempty(project.Sessions)
                    firstSessionId = project.Sessions(1).SessionId;
                    obj.selectSession(firstSessionId);
                end
                % =======================================================
            catch ME
                warning('ProjectExplorerPanel:RefreshFailed', ME.message);
            end
        end
```

---

### **2. WorkspaceManager.m 추가 패치**

**주요 개선 포인트**:
- WelcomeTab 강제 전환
- `addDashboardTab` 안정화
- `onTabChanged` 강화

#### **(1) `addDashboardTab` 메서드 끝부분 교체** (`obj.onTabChanged();` 이후):

```matlab
                obj.DashboardEntries(sessionId) = struct( ...
                    'SessionId', sessionId, ...
                    'Tab',       tab, ...
                    'Dashboard', dash);

                % ==================== [2026-05-14] WelcomeTab 강제 전환 ====================
                obj.TabGroup.SelectedTab = tab;
                obj.onTabChanged();

                % WelcomeTab이 여전히 선택되어 있으면 Dashboard로 강제 전환
                if ~isempty(obj.WelcomeTab) && isvalid(obj.WelcomeTab) && ...
                   isequal(obj.TabGroup.SelectedTab, obj.WelcomeTab)
                    obj.TabGroup.SelectedTab = tab;
                    obj.onTabChanged();   % 다시 호출하여 layout refresh
                end
                % =================================================================
```

#### **(2) `onTabChanged` 메서드 시작부분 강화**:

```matlab
        function onTabChanged(obj)
            try
                drawnow limitrate;   % ← UI 안정성 강화

                newId = obj.activeSessionId();
                if ~isempty(obj.App) && isvalid(obj.App)
                    obj.App.ActiveSessionId = newId;
                    if ~isempty(obj.App.StatusBar)
                        obj.App.StatusBar.setActiveSession(newId);
                    end
                end

                if isempty(newId) || strcmp(newId, 'standalone')
                    flightdash.util.SessionScope.clear();
                else
                    flightdash.util.SessionScope.setActive(newId);
                end

                obj.refreshActiveLayout('tabActivated');
                obj.refreshActiveInspector();
                obj.refreshActiveUndoUi();
            catch ME
                warning('WorkspaceManager:onTabChanged', ME.message);
            end
        end
```

---

**커밋 메시지 추천**

```bash
fix(studio): ProjectExplorer + WorkspaceManager 초기화 강화

- ProjectExplorerPanel: refreshFromProject에 expand + auto selectSession 추가
- WorkspaceManager: addDashboardTab에서 WelcomeTab → Dashboard 강제 전환
- onTabChanged에 drawnow limitrate 추가로 UI 안정성 개선
- Session 1 자동 생성 후 Explorer/Workspace 동기화 보강
```

